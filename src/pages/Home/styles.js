import styled from "styled-components";
import { containerStyle } from "../../styles/styleConstants";

const StyledMain = styled.main`
  padding-top: 4rem;
`;

const Container = styled.div`
  ${containerStyle}
`;

export { StyledMain, Container };
